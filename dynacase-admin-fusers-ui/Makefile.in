PACKAGE = dynacase-admin-fusers-ui
VERSION = @VERSION_FUSERS@
utildir = @PUBRULE@
pubdir = @prefix@
srcdir = @srcdir@
applib = FDL
appname = FUSERS
TAR = tar
GZIP_ENV = --best

export PACKAGE utildir pubdir applib appname

SUBDIR= Actions Apps Data Images Programs Scripts Zones

include $(utildir)/PubRule

clean:
	/bin/rm -f *.*~ config.* Makefile configure Apps/$(appname)/$(appname)_init.php info.xml ../$(PACKAGE)-*webinst LICENSE
	/bin/rm -fr autom4te.cache
